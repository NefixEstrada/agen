asyncapi: 3.0.0
info: { title: T, version: 1.0.0 }
channels:
  c:
    address: c
    messages:
      m: { payload: { type: string } }
operations:
  opOne:
    action: receive
    channel: { $ref: '#/channels/c' }
  op_one:
    action: receive
    channel: { $ref: '#/channels/c' }
