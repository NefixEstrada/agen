asyncapi: 3.0.0
info: { title: T, version: 1.0.0 }
channels:
  c:
    address: c
    messages:
      m:
        payload: { type: string }
operations:
  op:
    action: delete
    channel: { $ref: '#/channels/c' }
