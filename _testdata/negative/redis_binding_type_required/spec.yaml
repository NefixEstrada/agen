asyncapi: 3.0.0
info: { title: T, version: 1.0.0 }
channels:
  c:
    address: c
    x-redis: { maxLen: 100 }
    messages:
      m: { payload: { type: string } }
operations:
  op:
    action: receive
    channel: { $ref: '#/channels/c' }
