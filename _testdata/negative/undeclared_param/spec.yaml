asyncapi: 3.0.0
info: { title: T, version: 1.0.0 }
channels:
  c:
    address: 'things.{thingId}'
    messages:
      m: { payload: { type: string } }
operations:
  op:
    action: send
    channel: { $ref: '#/channels/c' }
