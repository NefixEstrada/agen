asyncapi: 3.0.0
info: { title: T, version: 1.0.0 }
channels:
  c:
    address: c
    messages:
      m: { payload: { type: string } }
operations:
  op:
    action: send
    summary: original
    channel: { $ref: '#/channels/c' }
    traits:
      - summary: from trait
